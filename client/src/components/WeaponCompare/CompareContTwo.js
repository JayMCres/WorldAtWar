import React, { Component } from "react";
import { Grid, Image, Segment, Header, Icon } from "semantic-ui-react";

export default class CompareTwo extends Component {
  render() {
    console.log("Compare Cont", this.props);
    return <Segment>ONE</Segment>;
  }
}
